Developer pushes code to Git (main / release)

CI pipeline triggers

Node.js app is built & tested

Docker image is built

Image is tagged (commit SHA / version / latest)

Image is pushed to Docker Hub
